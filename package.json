{
  "name": "constraint-management",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:solver": "vitest run tests/solver",
    "test:solver:coverage": "vitest run tests/solver --coverage",
    "bench:solver": "node scripts/solverBenchmark.js"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.10",
    "vite": "^5.4.10",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "xlsx": "^0.18.5"
  }
}